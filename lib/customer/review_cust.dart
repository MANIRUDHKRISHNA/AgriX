import 'package:flutter/material.dart';
class Reviewscreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Review and Ratings'),
      ),
      body: Center(
        child: Text('Welcome to the rivews and ratings Screen!'),
      ),
    );
  }
}